`$ cd framework/ && pnpm install && pnpm run dev` then observe error:

```
12:40:49 PM [vike][request(1)][Wrong Usage] No page found. You must create at least one page.
```

Despite the symlink `framework/(site)` containing pages.
